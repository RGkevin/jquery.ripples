window.jQuery(document).ready(function($){



	$('#example1').ripples(); // set the ripples effect
	$('#example2').ripples({
		frecuency : 2000,
		onmousemove : false
	}); // set the ripples effect

});
